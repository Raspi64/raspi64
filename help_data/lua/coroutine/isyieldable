coroutine.isyieldable
coroutine.isyieldable
Gibt true zurück wenn die Koroutine co yield kann. Der Standard für co ist die laufende Koroutine.
Eine Koroutine isyieldable wenn sie nicht der Main Thread oder sich in einer non-yieldable C-Funktion befindet.

Boolean =coroutine.isyieldable (co)

Beispiel:
Funktion a () do
   coroutine.yield(“Hello World”) 
end
co = coroutine.create(a)
coroutine.resume(co)
a = coroutine.isyiedable(co)



mid;mitte;middle;string;ausgabe;basic

MID:

Gibt von der Mitte aus die Anzahl von ASCII-Zeichen eines Strings zurück. 

	MID(String_1, Integer_1)
	Return String

Beispiel:
	
	s$ = "HELLO WORLD"
	PRINT MID(S$, 2)

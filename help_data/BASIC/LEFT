left;ascii;ausgabe;string;int;integer;basic

LEFT:

Gibt von links die Anzahl von ASCII-Zeichen eines Strings zurück. 

	LEFT(String_1, Integer_1)
	Return String

Beispiel:
	
	s$ = "HELLO WORLD"
	PRINT LEFT(S$, 2)

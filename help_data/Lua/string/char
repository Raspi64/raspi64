string.char;char;convert;ascii;string;lua

string.char
Erwartet 0 oder mehr Ganzzahlen. Liefert eine Zeichenkette mit einer Länge entsprechend der Anzahl der Argumente, wobei jedes Zeichen den gleichen numerischen Code besitzt, wie das korrespondierende Argument.

Numerischer Codes ist nicht notwendigerweise über verschiedene Plattformen portabel.

String = string.char(asci-wert,...)

Beispeil:
a = string.char(48,48,48)
print(a)  -- print “000”


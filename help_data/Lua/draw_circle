draw;circle;kreis;zeichnen;graphic;graphical output;grafische ausgabe;lua

Draw Circle (Kreis zeichnen)
Die "draw_circle" Funktion ist für das Zeichnen eines Kreises in der grafischen Ausgabe.
Der draw_circle Funktion müssen sechs Parameter mitgegeben werden:
- x und y sind number, welche beschreiben die Mitte des Kreises in Pixeln
- radius ist eine number, die beschreibt den Radius in Pixeln
- filled ist ein boolean, der beschreibt ob der Kreis ausgefuellt ist
- color ist eine table, welche beschreibt die Farbe, diese wird in Rot, Gruen, Blau, und Alpha (Intensität) angeben

Die Farbwerte, wie auch die Intensität, gehen von dem Wert 0 bis 255.

Beispiel:
local color = {}
color.red = 255
color.green = 100
color.blue = 0
color.alpha = 255

draw_circle(x, y, radius, thickness, filled, color)
draw_circle(10, 10, 30, 1, false, color)
draw_circle(15, 15, 10, 5, true, color)

type;nil;number;string;boolean;bool;table;tabelle;funktion;function;thread;thread;userdata;return;rückgabe;rueckgabe;convertlua

Type
Liefert den Typ des Wertes als String zurück. Mögliche Rückgabewerte dieser Funktion sind: “nil”, “number”, “string”, “boolean”, “table”, “funktion”, “thread” und “userdata”. 

String =  type(wert)

Beispiel:
a = 5
if  type(a)== “number” then
b = a+3
end


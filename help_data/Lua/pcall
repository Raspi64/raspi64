pcall;xpcall;geschützter modus;secure mode;secure;geschützt;geschuetzt;geschuetzter modus;lua;argumente;function;argument;parameter;funktion

Pcall
Ruft die Funktion f und die dazu angegeben Parameter im geschützten Modus auf. Fehler in dieser Methode werden nicht weitergereicht und pcall gibt stattdessen einen Statuscode zurück. Wenn der Statuscode, ein Boolean und “true” ist, ist die Methode ohne Fehler durchgelaufen. In diesem Fall gibt sie auch noch die Rückgabeparameter der Methode aus. Im Falle eines Errors wird ist der Boolean "false" und wird zusammen mit einem Error Objekt zurückgegeben.

Bollean, String = pcall(function,arg1,...)

Beispiel:
a,b = pcall(print(“hello world”))


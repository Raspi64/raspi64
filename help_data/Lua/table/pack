table.pack
table.pack
Gibt eine eine Tabelle zurück mit all den Parametern gespeichert in den Schlüsseln 1,2, etc. und dem Feld “n” mit der gesamten Anzahl an Parametern. Beachte die resultierende Tabelle ist keine Reihenfolge wenn eine Parameter nil sind.

Table =  table.pack (···)

Beispiel:
t = table.pack(7,3,2)
print(t[1]) -- print 7
print(t[2]) -- print 3

